# 🎯 نظام Onboarding للمستقلين (Freelancer Onboarding)

## 📋 **نظرة عامة**

عند تسجيل مستقل جديد، يتم توجيهه تلقائياً إلى صفحة **Onboarding** لإكمال معلومات ملفه الشخصي.

---

## 🔄 **سير العمل (Workflow)**

### **1. التسجيل كمستقل جديد:**

```
المستخدم يختار "مستقل" في صفحة التسجيل
    ↓
ملء البيانات الأساسية (الاسم، البريد، كلمة المرور)
    ↓
إنشاء الحساب بنجاح ✅
    ↓
التوجيه التلقائي إلى: /seller/onboarding
```

### **2. صفحة Onboarding (3 خطوات):**

#### **Step 1: Personal Information**
- الاسم الكامل
- الاسم المستعار (Display Name)
- صورة الملف الشخصي
- الوصف
- اللغات (Language + Level)

#### **Step 2: Professional Information**
- المهنة (Occupation)
- المهارات (Skills)
- التعليم (Education)
- الشهادات (Certifications)
- الموقع الشخصي (Portfolio Website)

#### **Step 3: Account Security**
- التحقق من البريد الإلكتروني
- إضافة رقم الهاتف
- التحقق من رقم الهاتف

### **3. بعد إكمال Onboarding:**

```
حفظ البيانات في localStorage
    ↓
تعيين profileCompleted = true
    ↓
التوجيه إلى: /freelancer/dashboard
    ↓
المستقل جاهز لتقديم العروض! 🚀
```

---

## 🔐 **التحقق من إكمال الملف الشخصي**

### **عند التسجيل الجديد:**
```javascript
// في AuthContext.js - register function
if (user.role === 'freelancer') {
  const profileCompleted = user.profileCompleted || 
                           localStorage.getItem(`profileCompleted_${user.id}`);
  if (!profileCompleted) {
    router.push('/seller/onboarding'); // ✅ توجيه لـ onboarding
  } else {
    router.push('/freelancer/dashboard'); // ✅ مباشرة للـ dashboard
  }
}
```

### **عند تسجيل الدخول:**
```javascript
// في AuthContext.js - login function
if (user.role === 'freelancer') {
  const profileCompleted = user.profileCompleted || 
                           localStorage.getItem(`profileCompleted_${user.id}`);
  if (!profileCompleted) {
    toast.info('يرجى إكمال معلومات ملفك الشخصي');
    router.push('/seller/onboarding'); // ✅ إكمال الملف أولاً
  } else {
    router.push('/freelancer/dashboard'); // ✅ جاهز للعمل
  }
}
```

---

## 💾 **حفظ البيانات**

### **في localStorage:**

```javascript
// بيانات الـ Onboarding
{
  "sellerProfile": {
    "personalInfo": {
      "fullName": "أحمد محمد",
      "displayName": "ahmed_dev",
      "profilePicture": null,
      "description": "مطور ويب محترف",
      "languages": [
        { "language": "Arabic", "level": "Native" },
        { "language": "English", "level": "Fluent" }
      ]
    },
    "professionalInfo": {
      "occupation": "Web Developer",
      "skills": ["React", "Node.js", "Next.js"],
      "education": [...],
      "certification": [...],
      "personalWebsite": "https://ahmed.dev"
    },
    "accountSecurity": {
      "emailVerified": true,
      "phoneVerified": true,
      "phone": "+966501234567"
    }
  }
}

// Flag الإكمال
localStorage.setItem(`profileCompleted_${userId}`, 'true');

// تحديث User Object
{
  "user": {
    "id": 1,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "role": "freelancer",
    "profileCompleted": true // ✅ مهم جداً
  }
}
```

---

## 📁 **الملفات المعدّلة**

### **1. contexts/AuthContext.js**
**التعديلات:**
- ✅ عند التسجيل: فحص `profileCompleted` وتوجيه للـ onboarding
- ✅ عند تسجيل الدخول: فحص `profileCompleted` قبل الوصول للـ dashboard

### **2. pages/seller/onboarding.js**
**التعديلات:**
- ✅ حفظ `profileCompleted = true` بعد إكمال النموذج
- ✅ التوجيه إلى `/freelancer/dashboard` بدلاً من `/seller/portfolio`
- ✅ تحديث user object في localStorage

---

## 🎯 **حالات الاستخدام (Use Cases)**

### **Case 1: مستقل جديد يسجل لأول مرة**
```
1. ملء نموذج التسجيل → اختيار "مستقل"
2. إنشاء الحساب بنجاح
3. التوجيه التلقائي إلى onboarding ✅
4. ملء 3 خطوات onboarding
5. حفظ البيانات
6. التوجيه إلى freelancer/dashboard ✅
7. جاهز لتقديم العروض! 🚀
```

### **Case 2: مستقل سجل ولكن لم يكمل onboarding**
```
1. تسجيل الدخول
2. فحص profileCompleted → false ❌
3. عرض toast: "يرجى إكمال معلومات ملفك الشخصي"
4. التوجيه إلى onboarding ✅
5. بعد الإكمال → dashboard
```

### **Case 3: مستقل أكمل onboarding سابقاً**
```
1. تسجيل الدخول
2. فحص profileCompleted → true ✅
3. التوجيه مباشرة إلى dashboard ✅
```

---

## ✅ **التحقق من الصلاحيات**

### **في صفحة Onboarding:**
```javascript
// يمكن لأي شخص مسجل الوصول
// لكن يُفضل للمستقلين فقط
const { user } = useAuth();

// اختياري: إضافة check
if (user?.role !== 'freelancer') {
  toast.warning('هذه الصفحة مخصصة للمستقلين');
  router.push('/');
}
```

### **في Freelancer Dashboard:**
```javascript
// التحقق من RBAC (موجود بالفعل)
const { isFreelancer, authLoading } = useAuth();

useEffect(() => {
  if (!authLoading && !isFreelancer) {
    router.push('/403');
  }
}, [authLoading, isFreelancer, router]);
```

---

## 📊 **الإحصائيات المتاحة**

بعد إكمال Onboarding، المستقل يمكنه:
- ✅ تصفح المشاريع المتاحة
- ✅ تقديم عروض على المشاريع
- ✅ إدارة عروضه في "My Offers"
- ✅ عرض وتعديل ملفه الشخصي
- ✅ إدارة المحفظة والسحب

---

## 🎨 **تحسينات مستقبلية (Optional)**

1. **Progress Bar:**
   - عرض نسبة إكمال الملف الشخصي
   - تشجيع المستقل على إكمال جميع الحقول

2. **Profile Strength Meter:**
   - "ضعيف" (معلومات أساسية فقط)
   - "جيد" (معلومات + بعض المهارات)
   - "ممتاز" (كل شيء مكتمل)

3. **Email Verification:**
   - إرسال email حقيقي للتحقق
   - تفعيل الحساب بعد التحقق

4. **Phone Verification:**
   - إرسال OTP عبر SMS
   - التحقق من رقم الهاتف

5. **Skip Option:**
   - السماح بتخطي onboarding
   - لكن مع قيود (مثلاً: لا يمكن تقديم عروض)

---

## 🧪 **كيفية الاختبار**

### **Test 1: تسجيل مستقل جديد**
1. اذهب إلى `/register`
2. املأ النموذج
3. اختر "مستقل (أقدم خدمات)"
4. أنشئ الحساب
5. **تحقق:** يجب أن تُوجَّه إلى `/seller/onboarding` ✅

### **Test 2: إكمال Onboarding**
1. املأ الخطوة 1 (Personal Info)
2. اضغط "التالي"
3. املأ الخطوة 2 (Professional Info)
4. اضغط "التالي"
5. املأ الخطوة 3 (Security)
6. اضغط "إنهاء"
7. **تحقق:** يجب أن تُوجَّه إلى `/freelancer/dashboard` ✅

### **Test 3: تسجيل دخول مستقل جديد (لم يكمل)**
1. سجّل خروج
2. سجّل دخول بحساب المستقل الجديد
3. **تحقق:** يجب أن تُوجَّه إلى `/seller/onboarding` ✅

### **Test 4: تسجيل دخول مستقل (أكمل onboarding)**
1. أكمل onboarding أولاً
2. سجّل خروج
3. سجّل دخول مرة أخرى
4. **تحقق:** يجب أن تُوجَّه مباشرة إلى `/freelancer/dashboard` ✅

---

## 🎓 **الفوائد**

1. **احترافية:** كل مستقل لديه ملف شخصي كامل
2. **ثقة:** العملاء يرون معلومات تفصيلية عن المستقلين
3. **تنظيم:** بيانات منظمة ومحفوظة بشكل جيد
4. **تجربة أفضل:** المستقل الجديد يفهم ما المطلوب منه

---

## 🚀 **الحالة الحالية**

✅ **مكتمل ويعمل!**

- ✅ تسجيل مستقل جديد → onboarding
- ✅ 3 خطوات onboarding
- ✅ حفظ البيانات
- ✅ تعيين profileCompleted
- ✅ التوجيه إلى dashboard
- ✅ فحص عند تسجيل الدخول

---

**🎉 النظام جاهز للاستخدام!**

