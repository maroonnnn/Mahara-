# 🧪 اختبار ظهور المشاريع للمستقلين

## الخطوات:

### 1. تأكد أن Backend يعمل

```bash
cd C:\Users\USER\Desktop\Back-end
php artisan serve
```

يجب أن ترى:
```
INFO  Server running on [http://127.0.0.1:8000].
```

### 2. تأكد أن Frontend يعمل

```bash
cd C:\Users\USER\Desktop\Front-end
npm run dev
```

يجب أن ترى:
```
- ready started server on 0.0.0.0:3000
```

### 3. اختبار إنشاء مشروع (كعميل)

1. **سجّل دخول كعميل:**
   - افتح `http://localhost:3000/login`
   - سجّل دخول بحساب عميل

2. **أنشئ مشروع جديد:**
   - اذهب إلى `/client/projects/new`
   - املأ النموذج:
     - العنوان: "مشروع تجريبي"
     - التصنيف: اختر أي تصنيف
     - الوصف: "وصف المشروع"
     - الميزانية: 100
     - مدة التنفيذ: 7 أيام
   - اضغط "نشر المشروع"

3. **تحقق من Console (F12):**
   - يجب أن ترى: `Project created via API: {...}`
   - يجب أن ترى: `Project created successfully`

### 4. اختبار عرض المشاريع (كمستقل)

1. **سجّل دخول كمستقل:**
   - سجّل الخروج من حساب العميل
   - سجّل دخول بحساب مستقل

2. **افتح لوحة التحكم:**
   - اذهب إلى `/freelancer/dashboard`
   - يجب أن ترى المشروع الجديد في قسم "أحدث المشاريع"

3. **تحقق من Console (F12):**
   - يجب أن ترى: `Projects API Response: {...}`
   - يجب أن ترى: `Projects List: [...]`
   - يجب أن يكون `totalProjects > 0`

### 5. اختبار API مباشرة

افتح PowerShell واكتب:

```powershell
# جلب المشاريع المفتوحة
$response = Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/projects/open" -Method Get -Headers @{"Accept"="application/json"}
$response | ConvertTo-Json -Depth 5
```

**يجب أن ترى:**
```json
{
  "current_page": 1,
  "data": [
    {
      "id": 1,
      "title": "مشروع تجريبي",
      "description": "وصف المشروع",
      "budget": 100,
      "status": "open",
      "offers_count": 0,
      "category": {...},
      "client": {...}
    }
  ],
  "total": 1
}
```

## استكشاف الأخطاء:

### المشكلة: لا توجد مشاريع في API

**الحل:**
1. تأكد أن المشروع تم إنشاؤه بنجاح
2. تحقق من Console في Frontend
3. تحقق من قاعدة البيانات:
   ```sql
   SELECT * FROM projects WHERE status = 'open';
   ```

### المشكلة: المشاريع لا تظهر في Frontend

**الحل:**
1. افتح Console (F12)
2. ابحث عن: `Projects API Response`
3. تحقق من:
   - `response.data.data` يجب أن يكون array
   - `response.data.total` يجب أن يكون > 0

### المشكلة: خطأ في API

**الحل:**
1. تأكد أن Backend يعمل على `http://127.0.0.1:8000`
2. تأكد من ملف `.env.local`:
   ```env
   NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api
   ```
3. تحقق من CORS في `Back-end/config/cors.php`

## ملاحظات:

✅ **تم إصلاح:**
- Parsing pagination response في Frontend
- Console logging للمساعدة في debugging
- Handling مختلف response formats

✅ **النظام يعمل:**
- عند إنشاء مشروع → يُحفظ مع `status = 'open'`
- عند جلب المشاريع → تظهر جميع المشاريع المفتوحة
- المشاريع الأحدث تظهر أولاً

